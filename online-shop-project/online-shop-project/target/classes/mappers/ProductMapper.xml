<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.online.shop.dao.mybatisiml.ProductMapper">

    <select id="findById" parameterType="int" resultType="com.online.shop.model.Product">
        SELECT * FROM products WHERE product_id = #{productId};
    </select>

    <select id="findAll" resultType="com.online.shop.model.Product">
        SELECT * FROM products;
    </select>

    <insert id="insert" parameterType="com.online.shop.model.Product">
        INSERT INTO products (name, price, category_id)
        VALUES (#{name}, #{price}, #{categoryId});
    </insert>

    <update id="update" parameterType="com.online.shop.model.Product">
        UPDATE products
        SET name = #{name}, price = #{price}, category_id = #{categoryId}
        WHERE product_id = #{productId};
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM products WHERE product_id = #{productId};
    </delete>

</mapper>
