# Online Shop - Plain Maven Project
This project is autogenerated by ChatGPT. It includes:

- ConnectionPool (reads `src/main/resources/db.properties`)
- 5 services: UserService, ProductService, OrderService, InventoryService, PaymentService
- `Main.java` demonstrates usage using only services.

Database: MySQL  
Default DB properties in `src/main/resources/db.properties`:
```
db.url=jdbc:mysql://localhost:3306/online_shop_project?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.user=root
db.password=root
db.poolSize=10
```

Open the folder in IntelliJ/Eclipse as a Maven project. Build with `mvn clean package`.
